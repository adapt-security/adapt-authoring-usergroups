# adapt-authoring-usergroups

Manages groups of [users](../adapt-authoring-users). A group collects users together so resources (such as courses) can be shared with the group rather than with each user individually.

Extends `AbstractApiModule` from [adapt-authoring-core](../adapt-authoring-core).
